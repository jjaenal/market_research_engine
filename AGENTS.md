# AGENTS.md

Market Research Engine (MRE): a documentation-driven, event-driven framework for testing trading hypotheses (RSI Trendline Breakout baseline). **Phase M7 — Iteration is CLOSED** (per git history + FND-008 §7); M0–M7 are done. Next phase: Research/Experiment — **EXP-002 complete (TODO-035/036/037); SUPPORTED at venue costs but temporally non-stationary**; **EXP-003 complete (TODO-038/039); SUPPORTED per pre-registered criteria — edge concentrated in HIGH volatility regime, stationary train+test at venue costs**; **EXP-003 tradable-validation addendum done (EXP-003 §17.5/§17.6/§18.4, doc 1.0.3): 8-slice robustness 4/8 positive, split-point OOS 3/4 stationary, combined filter 4/5 non-extreme combos positive; formal status BELUM TRADABLE (EXP-003 §18.5); newer-spot-data evaluation DEFERRED (spot XAUUSD H1 past 2026-05-26 unavailable from reliable free sources)**; **EXP-004 pre-registered (TODO-040, doc 1.0.0): re-test the high-regime edge with ATR-multiple SL/TP (SL 1.0/TP 4.0) at real venue costs 1.0 bps/side — M7 only tested SL/TP on the synthetic 2–5 bps/side grid (RQ-007 TIDAK)**; **EXP-004 ran (TODO-041, doc 1.0.1): REJECTED per pre-registered criteria — SL 1.0/TP 4.0 @ 1.0 bps/side → expectancy 1.1654 (n=698 ≥ 30), breakeven ≈ 3.31 bps/side < 3.44 bps control (hypothesis "SL/TP raises cost tolerance" NOT supported), OOS train +0.2026 & test +2.9515 (stationary), robustness improved (3/4 slices, 6/8 fine, 5/5 combos, 4/4 split-point) — SL/TP value as an exit rule, not cost-tolerance (EXP-004 §15–§18)**; **RSI Trendline Breakout research line CLOSED (EXP-001..EXP-004): edge does not show sufficient tradable profile at realistic venue costs; researcher decision = pre-register a NEW strategy, not more parameter mining (EXP-004 §18.3)**; **EXP-005 pre-registered (TODO-042, doc 1.0.0): NEW strategy line — Price Breakout (Donchian-style), pure price momentum (PRICE_CONFIRMATION + SWING_HIGH), no RSI; plugin `price_breakout` in `src/mre/strategies/exp005.py` (ARC-ACT-010, no architecture change); config frozen identical to EXP-002 (venue cost 1.0 bps/side)**; **EXP-005 ran (TODO-043, doc 1.0.1): REJECTED per pre-registered criteria — baseline @ 1.0 bps/side → expectancy −3.4848 (n=3882 ≥ 30), PF 0.428, win rate 0.337; expectancy negative even at ZERO cost (−3.1186 @ comm=0/slip=0) → gross edge absent, breakeven < 0 bps/side; OOS train −2.6301 & test −5.2396 (both negative); 0/4 temporal slices, 0/5 parameter combos, XAGUSD −0.1232; 0/4 criteria met (EXP-005 §15–§18) — structural failure, not a cost artifact; strengthens cross-line conclusion that XAUUSD H1 edge is not tradable at realistic venue costs across two distinct strategy classes (EXP-005 §18.2)**; **EXP-006 ran (TODO-044/045, doc 1.0.1): REJECTED per pre-registered criteria — Price Breakout re-tested on XAUUSD H4 (separate export `datasets/XAUUSD_H4.csv`, 26,816 candles, 2009-07-13..2026-04-14 — NOT aggregated from H1; config frozen identical to EXP-005 §9 except timeframe → H4; venue cost 1.0 bps/side): baseline expectancy −8.3297 (n=1188 ≥ 30), PF 0.429; negative even at ZERO cost (−7.9576) → breakeven < 0 bps/side; OOS train −5.0451 & test −14.2008 (both negative, test worse +181.5%); 0/4 temporal slices, 0/5 parameter combos, XAGUSD −0.1232; 0/4 criteria (EXP-006 §15–§18) — hypothesis "edge specific to H4" NOT supported, failure is cross-timeframe (H1 + H4); **Price Breakout research line CLOSED (EXP-006 §18.3)**; **EXP-007 pre-registered (TODO-046, doc 1.0.0): NEW strategy class — Swing Breakout (Fractal Structure) on XAUUSD H4 — swing-high fractal sets a structural resistance level (SWING_HIGH trigger), then a price confirmation breaks it within the window (PRICE_CONFIRMATION confirmation); entry at the breakout candle; structural complement of Price Breakout (reversed trigger/confirmation order); plugin `swing_breakout` in `src/mre/strategies/exp007.py` (ARC-ACT-010, consumes only existing Event types, no engine change); config frozen identical to EXP-006 §9 except strategy_id (venue cost 1.0 bps/side, same `datasets/XAUUSD_H4.csv`); run = TODO-047 (pending)**.

## Current state (trust git history + FND-008 §7 TODO table; FND-006 prose lags)
- **Code and tests exist.** Package is `src/mre/` (`core`, `strategies`, `detectors`, `engines`, `indicators`, `loaders`, `models`, `utils`). The strategy plugin `strategies/exp001.py` registers the RSI Trendline Breakout signal definition under `EXP001_STRATEGY_ID`; `ExperimentConfig.strategy_id` resolves it from the registry (ARC-ACT-010).
- **M7 is CLOSED.** RQ-006 (dedup + regime) and RQ-007 (SL/TP ATR-multiple) are both ANSWERED TIDAK — the edge does not survive ≥ 0.05%/side at realistic costs under any mitigation tried (ARC-008 §14.1/§14.3, EXP-001 §19.6/§19.7). Final verdict (ARC-008 §14.4, EXP-001 §19.8): hypothesis EXP-001 REJECTED as a tradable strategy at realistic costs; edge only survives at zero/near-zero cost. **EXP-002 ran (TODO-036) and is SUPPORTED per pre-registered criteria** — re-testing the edge against real retail XAUUSD venue execution costs (spread + commission + slippage ECN ≈ 1.0 bps/side) instead of the synthetic 2–5 bps/side grid M7 used (`configs/EXP-002.yaml`, `docs/07-experiments/EXP-002_RSI_Trendline_Breakout_Real_Venue_Cost.md`): representative 1.0 bps/side → expectancy 0.5111 (n=1403 ≥ 30), breakeven ≈ 2.43 bps/side, whole 0.5–2.0 bps/side grid positive (EXP-002 §15/§18); **OOS/robustness done (TODO-037)**: test exp 1.9810 positive OOS but not stationary — train negative (−0.1605), 1/4 temporal slices positive at venue cost; XAGUSD thin positive; verdict SUPPORTED at venue costs but insufficient evidence as tradable yet (EXP-002 §16/§17/§18.3); **EXP-003 ran (TODO-039) and is SUPPORTED per pre-registered criteria** — volatility regime segmentation: `configs/EXP-003.yaml` (venue cost 1.0 bps/side identical to EXP-002 + `regime.selected_regime: "high"` resolved by `select_regime()` at `src/mre/core/experiment_runner.py:103`; pre-registration `docs/07-experiments/EXP-003_RSI_Trendline_Breakout_Volatility_Regime.md`): regime high → expectancy 0.8887 @ 1.0 bps/side (n=698 ≥ 30), breakeven ≈ 3.44 bps/side (vs 2.43 unfiltered), OOS train +0.1297 & test +2.4853 (stationary), 2/4 temporal slices positive (vs 1/4), 4/5 combos positive (vs 3/5); verdict SUPPORTED — edge concentrated in HIGH regime, stationarity improved (EXP-003 §15/§18); **tradable validation done (EXP-003 §17.5/§17.6/§18.4)**: finer 8-slice robustness 4/8 positive (consistent with 2/4 coarse), split-point OOS stationary at 3/4 splits (0.5/0.6/0.7), combined filter (high regime + non-extreme params) 4/5 combos positive; formal status BELUM TRADABLE (EXP-003 §18.5); newer-spot-data evaluation DEFERRED — spot XAUUSD H1 past 2026-05-26 unavailable from reliable free sources (Yahoo XAUUSD=X delisted, Dukascopy 503/404/timeout, Stooq/Investing JS challenge); GC=F futures is a different instrument/venue-cost model so cannot substitute; evidence supports but does not conclude tradable; **EXP-004 pre-registered (TODO-040, doc 1.0.0, `configs/EXP-004.yaml`): re-test the high-regime edge with ATR-multiple SL/TP (SL 1.0/TP 4.0, RQ-007 machinery) at real venue costs 1.0 bps/side — M7 only tested SL/TP on the synthetic 2–5 bps/side grid (RQ-007 TIDAK); frozen config identical to EXP-003 + SL/TP; decision criteria (EXP-004 §13): expectancy > 0, breakeven >= 3.44 bps/side, OOS train+test positive; run = TODO-041 DONE (doc 1.0.1): REJECTED per pre-registered criteria — breakeven ≈ 3.31 < 3.44 bps control (SL/TP does NOT raise cost tolerance), though expectancy +31%, Max DD −61%, OOS stationary, robustness improved (EXP-004 §15–§18); newer-data path does NOT block further experiments**; **RSI Trendline Breakout line CLOSED (EXP-004 §18.3)** and **EXP-005 pre-registered (TODO-042, doc 1.0.0, `configs/EXP-005.yaml`, plugin `price_breakout`): NEW strategy line — Price Breakout (Donchian-style), pure price momentum, no RSI; config frozen identical to EXP-002 (1.0 bps/side); decision criteria (EXP-005 §13): expectancy > 0, breakeven >= 1.0 bps/side, OOS train+test positive; run = TODO-043 (pending)**; **EXP-005 ran (TODO-043, doc 1.0.1): REJECTED per pre-registered criteria — baseline @ 1.0 bps/side → expectancy −3.4848 (n=3882 ≥ 30), PF 0.428, win rate 0.337; expectancy negative even at ZERO cost (−3.1186 @ comm=0/slip=0) → gross edge absent, breakeven < 0 bps/side; OOS train −2.6301 & test −5.2396 (both negative); 0/4 temporal slices, 0/5 parameter combos, XAGUSD −0.1232; 0/4 criteria met (EXP-005 §15–§18) — structural failure, not a cost artifact; strengthens cross-line conclusion that XAUUSD H1 edge is not tradable at realistic venue costs across two distinct strategy classes (EXP-005 §18.2)**; **EXP-006 ran (TODO-044/045, doc 1.0.1, `configs/EXP-006.yaml`): Price Breakout re-tested on XAUUSD H4 (`datasets/XAUUSD_H4.csv`, 26,816 candles, 2009-07-13..2026-04-14 — separate export, NOT aggregated from local H1) to test whether the edge is timeframe-specific; config frozen identical to EXP-005 §9 except timeframe → H4 (venue cost 1.0 bps/side); decision criteria (EXP-006 §13): expectancy > 0, breakeven >= 1.0 bps/side, OOS train+test positive; result REJECTED per pre-registered criteria — baseline expectancy −8.3297 (n=1188 ≥ 30), negative even at ZERO cost (−7.9576) → breakeven < 0 bps/side, OOS train −5.0451 & test −14.2008 (both negative), 0/4 slices, 0/5 combos, XAGUSD −0.1232; hypothesis "edge specific to H4" NOT supported — failure is cross-timeframe; line Price Breakout CLOSED (EXP-006 §18.3); **EXP-007 pre-registered (TODO-046, doc 1.0.0, `configs/EXP-007.yaml`, plugin `swing_breakout`): NEW strategy class — Swing Breakout (Fractal Structure) on XAUUSD H4 — swing-high fractal sets a structural resistance level (SWING_HIGH trigger), then price confirmation breaks it within the window (PRICE_CONFIRMATION confirmation), entry at breakout candle; structural complement of Price Breakout (reversed trigger/confirmation order); config frozen identical to EXP-006 §9 except strategy_id (venue cost 1.0 bps/side); decision criteria (EXP-007 §13): expectancy > 0, breakeven >= 1.0 bps/side, OOS train+test positive; run = TODO-047 (pending)**.**. ARC-ACT-010 (Extract Strategy Plugin Package — `src/mre/strategies/` registry + `exp001.py`), ARC-ACT-011 (Move Experiment Config to External File — `configs/EXP-001.yaml` + `load_experiment_config()` in `src/mre/core/experiment_runner.py`, `--config` CLI flag), ARC-ACT-012 (Signal Deduplication — `SignalRule.cooldown` in `src/mre/models/signal_rule.py`, implemented in `combine()` at `src/mre/engines/signal_engine.py`, ENG-003 §8.1), ARC-ACT-013 (Unify Segment Runner — `src/mre/core/segments.py`: `run_on_slice()`, `SegmentRun`, `ensure_normalized()`, shared by OOS + robustness), and ARC-ACT-014 (Unify Renderers & Config Builder — `src/mre/utils/markdown.py` `heading`/`table`, `exp001_config()` in `src/mre/core/experiment_runner.py`, `src/mre/cli.py`) are DONE. The re-run machinery added in M7 iteration: `SignalRule.cooldown` override via `signal_cooldown` + `regime` section in the config YAML, `RegimeConfig` + `select_regime()` in `src/mre/core/experiment_runner.py`, the ATR volatility regime indicator `src/mre/indicators/regime.py` (`volatility_regime()`), and ATR-multiple SL/TP (`stop_loss_atr`/`take_profit_atr`/`atr_period` in `ExecutionConfig`, resolved at entry bar in `simulate()` via `_resolve_stop_take()`). TODO-025..028 and the M7 architecture review (ARC-008 — **CORE HOLDS, PERIPHERY DRIFTS**) are complete; M6 validation done.
- Foundation docs are **locked** (FND-010, approved). Docs are written in **Indonesian**; use FND-009 glossary terms (Event ≠ Signal ≠ Trade).
- **Docs drift is a known risk (R-005):** FND-006's status snapshot still says "M1 / Not Started" while its milestone table and FND-008 §7 say M6. When they conflict, trust git history and the FND-008 master TODO table.
- `datasets/`, `experiments/`, `reports/` are **gitignored but populated locally** (XAUUSD_H1.csv ~100k rows; EXP-001 report/sensitivity under `experiments/EXP-001/`). Never commit them. Tests use synthetic sine CSVs, never real data.

## Commands (exact)
- Run all tests: `.venv/bin/python -m pytest` (pyproject.toml sets `testpaths=["tests"]` and `pythonpath=["src"]`; pytest 9.1.1 installed in `.venv`).
- Run one file: `.venv/bin/python -m pytest tests/test_out_of_sample.py`
- Run a CLI module — **must** prepend `PYTHONPATH=src`; plain `python -m mre.core.*` raises `ModuleNotFoundError` because the package is not pip-installed (pyproject.toml has no `[project]` section):
  - **Unified entrypoint (ARC-ACT-014):** `PYTHONPATH=src .venv/bin/python -m mre.cli <subcommand>` where `<subcommand>` ∈ `baseline|sensitivity|oos|robustness`. The old per-module entrypoints still work (they delegate to `mre.cli`):
  - `PYTHONPATH=src .venv/bin/python -m mre.core.experiment_runner` — EXP-001 baseline (M5, done)
  - `PYTHONPATH=src .venv/bin/python -m mre.core.sensitivity` — TODO-024 (done)
  - `PYTHONPATH=src .venv/bin/python -m mre.core.out_of_sample` — TODO-025 (done)
  - `PYTHONPATH=src .venv/bin/python -m mre.core.robustness` — TODO-026 (done; `--no-market` to skip XAGUSD)
- **No linter/formatter is configured** (ruff/black are planned in DEV-002, not installed). Don't invent `ruff`/`black`/`mypy`/`flake8` commands.

## Architecture notes (not obvious from filenames)
- Pipeline: `normalize_raw_csv` → `load_dataset` → indicators (`rsi`) → `EventEngine.detect` → `signal_engine.combine` → `select_regime` (optional ATR regime filter) → `simulate` → `calculate` → `render` (markdown). Orchestrated in `compute_report()` (`src/mre/core/experiment_runner.py:63`).
- **Config is frozen dataclasses built from an external YAML file** (`ExperimentConfig`, `EventEngineConfig`, `ExecutionConfig`, `StatisticsConfig`, `RegimeConfig`); frozen EXP-001 parameters live in `configs/EXP-001.yaml` (committed, single source of truth — FR-012), loaded by `load_experiment_config()` (`src/mre/core/experiment_runner.py`) and wrapped by `exp001_config()`, which every CLI subcommand builds from (`--config` flag, ARC-ACT-011/014). `signal_cooldown` (dedup override) and the `regime` section (ATR volatility filter) are applied in `ExperimentConfig.__post_init__`/`compute_report()` via `select_regime()`. `ExecutionConfig` carries both absolute SL/TP (`stop_loss`/`take_profit`) and ATR-multiple SL/TP (`stop_loss_atr`/`take_profit_atr`/`atr_period`, RQ-007); `simulate()` resolves levels at the entry bar via `_resolve_stop_take()` (no lookahead).
- **Strategies are plugins** (ARC-005 §6, ARC-ACT-010): `src/mre/strategies/` (`registry.py` — `register`/`get`/`registered_ids`; `exp001.py` registers `EXP001_STRATEGY_ID`). `ExperimentConfig.strategy_id` resolves `signal_definition` from the registry; the Signal Engine interface (`combine(events, signal_definition)`) is unchanged — add a strategy by registering, never by editing the engine.
- Engines implemented: event, signal, simulation, statistics, reporting. ENG-004 Probability Engine has no doc or impl (statistics engine covers it). Detectors: swing, price_confirmation, rsi_trendline. Indicators: rsi, ema, atr, regime (`volatility_regime()` — ATR short vs long → "high"/"low"/None warm-up, ARC-008 §14).
- Renderers (`reporting_engine.render` → `report.to_markdown`, `sensitivity.to_markdown`, `out_of_sample.to_markdown`, `robustness.to_markdown`) are pure string output and share the markdown helpers `heading`/`table` in `src/mre/utils/markdown.py` (ARC-ACT-014); file writes happen only in `run_experiment()` and CLI `main()`s.
- `mre/core/segments.py` (`run_on_slice`, `SegmentRun`, `ensure_normalized`) is the shared segment runner — OOS train/test and robustness period splits both go through it. It uses `mre/utils/candle_csv.py` (`write_candle_csv`) as the compliant segment CSV writer — reuse `run_on_slice`/`write_candle_csv` instead of hand-writing segment CSVs.

## Doc governance (when touching docs)
- New/updated docs need an ID from the FND-004 registry; creating or versioning a doc means syncing FND-004. ADRs go in `docs/06-decisions/`. Major architectural decisions require an ADR before implementation.
- Docs follow FND-002 front-matter/structure and are in **Indonesian**.
- Changes land via short-lived `feat/mN-*` branches merged to `main` through PRs (see git history).

## Coding standards (verified in the code)
- Type hints everywhere; frozen dataclasses for config; docstring on every public function/class; pure deterministic functions — no time/random sources, no future-candle lookahead (reproducibility is a hard requirement, Article 7).
- No `print` in business logic; `print` only in CLI `main()` entrypoints.
- One test file per module with `test_*` names and synthetic deterministic fixtures; correctness > performance.
